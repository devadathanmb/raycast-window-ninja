{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "window-ninja",
  "title": "Window Ninja",
  "description": "Switch between every open window across all apps and all Spaces",
  "icon": "command-icon.png",
  "author": "devadathanmb",
  "categories": [
    "System",
    "Productivity"
  ],
  "license": "AGPL-3.0-only",
  "commands": [
    {
      "name": "window-ninja",
      "title": "Window Ninja",
      "description": "List and switch between every open window across all apps and Spaces",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "showMinimizedWindows",
      "title": "Show Minimized Windows",
      "description": "Show minimized (hidden) windows in the list",
      "type": "checkbox",
      "default": false,
      "label": "Show Minimized Windows",
      "required": false
    },
    {
      "name": "showApplicationsWithoutVisibleWindows",
      "title": "Show Applications Without Visible Windows",
      "description": "Show windows belonging to applications hidden with macOS's Hide Application action",
      "type": "checkbox",
      "default": true,
      "label": "Show Applications Without Visible Windows",
      "required": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.24"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^22.19.17",
    "@types/react": "^19.2.18",
    "eslint": "^10.8.1",
    "husky": "^9.1.7",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "scripts": {
    "build:swift": "cd win-ninja && swift build -c release && cp .build/release/win-ninja ../assets/win-ninja",
    "build": "npm run build:swift && ray build -o dist",
    "dev": "npm run build:swift && ray develop",
    "fix-lint": "eslint src --fix && prettier --write . && npm run format:swift",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint src && prettier --check . && swift format lint -r win-ninja/Sources/",
    "format:swift": "swift format -i -r win-ninja/Sources/",
    "typecheck": "tsc --noEmit",
    "prepare": "husky"
  }
}
